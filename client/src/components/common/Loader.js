import React from 'react';
import src from '../../assets/img/loader.gif';

const Loader = () => (
    <div className="loader">
        <img src={src} alt="Loader" />
    </div>
);

export default Loader;